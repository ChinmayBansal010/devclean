# Doctor Checks

`doctor` checks developer tooling without changing the machine.

Checks report presence, version availability, and basic executable health. A missing optional tool is informational; a malformed configuration or unusable required dependency is actionable. The command must never delete caches or rewrite configuration.

JSON output uses stable check identifiers.