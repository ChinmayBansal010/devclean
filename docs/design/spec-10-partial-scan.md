# Partial Scan Results

A scan may complete with partial results when individual cache sources fail.

The result records completion state and per-cache diagnostics. Partial results must not be presented as a fully verified inventory. Cleanup based on partial results should remain conservative and should require explicit user intent.