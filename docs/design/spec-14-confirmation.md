# Cleanup Confirmation

Interactive cleanup confirmation summarizes selected caches, expected recovery, safety exclusions, and active filters.

Confirmation occurs after planning and before mutation. It must never alter the plan. Non-interactive cleanup requires an explicit flag when destructive action is requested.