# Active Process Detection

Process detection is advisory evidence for cache activity and safety.

A failed process query must not be interpreted as proof that no process is active. The cleaner therefore prefers conservative exclusion when a cache policy requires process awareness and detection is unavailable.