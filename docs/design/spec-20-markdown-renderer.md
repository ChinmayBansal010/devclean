# Markdown Renderer

Markdown output is intended for reports and issue attachments rather than terminal interaction.

Tables should remain deterministic, headings should follow the same command hierarchy, and machine values should not be localized. The renderer consumes normalized results and performs no policy decisions.