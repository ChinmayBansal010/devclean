# Terminal Output

Interactive output favors concise summaries with optional verbose diagnostics.

Colors and symbols are presentation hints only. Important safety information must remain visible when color is disabled. Width-sensitive rendering should degrade to plain text instead of truncating critical paths or decisions.