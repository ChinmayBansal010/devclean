# Quiet Mode

Quiet mode suppresses informational output while preserving errors and explicit cleanup summaries required for safe operation.

It is intended for scripts and scheduled jobs. Machine-readable output should be preferred when a caller needs structured results.