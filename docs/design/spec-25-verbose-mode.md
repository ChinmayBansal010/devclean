# Verbose Mode

Verbose mode exposes diagnostic stages, policy decisions, and aggregate timings without changing behavior.

Detailed output must not include secrets or unrestricted environment variables. Per-file tracing remains opt-in because it can produce large output and sensitive paths.