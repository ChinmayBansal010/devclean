# Cache Identity

Cache identity combines a stable provider identifier with a cache name and normalized scope.

Identity must survive changes in filesystem size and timestamps. It is used by history comparison, recommendation state, and report grouping.