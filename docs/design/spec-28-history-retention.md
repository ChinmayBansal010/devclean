# History Retention

Scan history should retain enough snapshots for trend analysis without growing indefinitely.

Retention policy is configurable and should remove only old history records, never cache data. A retention action reports how many records were removed and the remaining history span.