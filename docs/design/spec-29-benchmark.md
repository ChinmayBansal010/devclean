# Benchmark Contract

Benchmarks measure scan throughput and cleanup-planning overhead separately.

Benchmark runs should record platform, compiler, build type, dataset shape, and timing. Results are comparative signals rather than fixed performance guarantees.