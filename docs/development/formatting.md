# Formatting

C++ sources follow the repository `.clang-format` configuration. Run the formatter helper before committing formatting changes:

```bash
scripts/format.sh
```

Review the resulting diff before committing so formatting does not hide functional changes.
