# Local installation

After a successful build, install the executable into a user-local directory without requiring root:

```bash
scripts/install-local.sh
```

The default destination is `$HOME/.local/bin`. A custom prefix can be supplied as the first argument.
