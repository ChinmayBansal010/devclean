# Benchmark Practice

Benchmarks should report the workload shape as well as elapsed time. At minimum, record the number of scanned entries, candidate count, reclaimed-byte estimate, build configuration, and platform.

Comparisons are meaningful only when the workload and configuration remain comparable.
