# Cache Accounting

Cache discovery can encounter overlapping paths or equivalent artifacts. Duplicate candidates should be accounted for before aggregate size calculations and cleanup planning.

The identity used for accounting should remain stable across repeated scans and should not depend on presentation formatting.
