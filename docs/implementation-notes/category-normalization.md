# Category Normalization

Cache categories should be normalized before matching filters or generating aggregate reports.

Case differences and presentation labels should not create separate internal categories. Unknown categories should remain distinguishable from an empty filter.
