# CI Debugging

When a CI job fails, reproduce the same build preset and test target locally before changing unrelated code.

Separate configuration failures, dependency failures, compiler diagnostics, test failures, and packaging failures. The first failing stage is usually the most useful signal.
