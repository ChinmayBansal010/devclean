# Dependency Updates

Dependency updates should be isolated from unrelated behavior changes so failures can be attributed clearly.

After an update, verify configuration, compilation, unit tests, integration tests, sanitizer builds, and packaging before considering the dependency change complete.
