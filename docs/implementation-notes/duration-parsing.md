# Duration Parsing

Time thresholds should be normalized into a single internal unit before policy evaluation.

Parsing should reject malformed values and protect against arithmetic overflow. The user-facing syntax should remain consistent across scan, analyze, and cleanup commands.
