# Exit Status Usage

Exit codes should make DevClean useful in shell scripts and CI.

Successful scans and cleanups should remain distinguishable from invalid arguments, operational failures, and incomplete work. Documentation and examples should use the same status semantics as the executable.
