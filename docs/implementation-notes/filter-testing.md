# Filter Testing

Filter tests should cover combinations of category, size, age, target, exclusion, and sort options.

Each test should verify both candidate selection and the resulting aggregate values so that a filter change cannot silently alter reporting semantics.
