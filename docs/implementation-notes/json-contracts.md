# JSON Contracts

JSON output is an API surface. Field names, value types, and required fields should remain stable within a supported contract version.

When a schema changes incompatibly, update the documented version and provide a migration path rather than silently changing existing fields.
