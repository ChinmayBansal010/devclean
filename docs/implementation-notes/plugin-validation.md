# Plugin Validation

Plugins should be rejected early when their metadata is incomplete or incompatible with the scanner contract.

Validation should cover identity, version compatibility, supported categories, required fields, and duplicate registration. Invalid plugins must not partially enter the active registry.
