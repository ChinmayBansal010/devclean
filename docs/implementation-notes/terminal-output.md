# Terminal Output

Interactive output should remain readable when the terminal width is small or when standard output is redirected.

Human-readable formatting belongs at the presentation layer. Machine-readable modes should avoid decorative text that can corrupt downstream parsing.
